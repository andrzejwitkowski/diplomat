=== infrastructure/src/main/kotlin/pl/diplomat/infrastructure/adapter/OpenAiCompatibleLlmAdapter.kt : line 27 ===
--- a/infrastructure/src/main/kotlin/pl/diplomat/infrastructure/adapter/OpenAiCompatibleLlmAdapter.kt
+++ b/infrastructure/src/main/kotlin/pl/diplomat/infrastructure/adapter/OpenAiCompatibleLlmAdapter.kt
@@ -17,20 +17,20 @@
   * org.json, mirroring [pl.diplomat.infrastructure.ota.OtaUpdateManager].
   */
  class OpenAiCompatibleLlmAdapter : LlmCompletionPort {
  
      override suspend fun complete(
          settings: LlmSettings,
          messages: List<ChatMessage>,
      ): LlmCompletionResult = withContext(Dispatchers.IO) {
          val connection = (URL(endpoint(settings.baseUrl)).openConnection() as HttpURLConnection).apply {
              requestMethod = "POST"
>             connectTimeout = 30_000
              readTimeout = 60_000
              doOutput = true
              setRequestProperty("Content-Type", "application/json")
              setRequestProperty("Authorization", "Bearer ${settings.apiKey}")
          }
          try {
              connection.outputStream.use { output ->
                  output.write(buildRequestBody(settings.model, messages).toByteArray(Charsets.UTF_8))
              }
# Comment: dodaj to jako stala

=== infrastructure/src/main/kotlin/pl/diplomat/infrastructure/adapter/OpenAiCompatibleLlmAdapter.kt : line 28 ===
--- a/infrastructure/src/main/kotlin/pl/diplomat/infrastructure/adapter/OpenAiCompatibleLlmAdapter.kt
+++ b/infrastructure/src/main/kotlin/pl/diplomat/infrastructure/adapter/OpenAiCompatibleLlmAdapter.kt
@@ -18,20 +18,20 @@
   */
  class OpenAiCompatibleLlmAdapter : LlmCompletionPort {
  
      override suspend fun complete(
          settings: LlmSettings,
          messages: List<ChatMessage>,
      ): LlmCompletionResult = withContext(Dispatchers.IO) {
          val connection = (URL(endpoint(settings.baseUrl)).openConnection() as HttpURLConnection).apply {
              requestMethod = "POST"
              connectTimeout = 30_000
>             readTimeout = 60_000
              doOutput = true
              setRequestProperty("Content-Type", "application/json")
              setRequestProperty("Authorization", "Bearer ${settings.apiKey}")
          }
          try {
              connection.outputStream.use { output ->
                  output.write(buildRequestBody(settings.model, messages).toByteArray(Charsets.UTF_8))
              }
              val code = connection.responseCode
# Comment: dodaj jako stala

